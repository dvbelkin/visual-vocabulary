import { defineConfig } from "astro/config";

export default defineConfig({
    site: "https://dvbelkin.github.io",
    base: "/visual-vocabulary",
    output: "static",
    trailingSlash: "always",
});
